const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    extend: {
      fontFamily: {
        sans: ["Syne-Bold", "sans-serif"],
      },
    },
  },
};

export default config;
